[phases.setup]
nixPkgs = ['python311', 'pip']

[phases.install]
cmds = ['ls -la', 'pwd', 'pip install --upgrade pip', 'pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'python bot.py'
